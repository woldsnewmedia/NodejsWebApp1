<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>

    <header>
        <%- include('../partials/header') %>
    </header>

    <div class="container">

        <h1>API QS</h1>

        <p>This is just to test using Node to get some test data from an API using a Key in the QueryString.</p>

        <p>API URL: <span class="font-weight-bold"><%=api_url%></span></p>

        <div class="card shadow">
            <div class="card-header">
                API Jobs
            </div>
            <div class="card-body">
                <% api_data.forEach((p, index) => { %>

                    <div class="row">
                        <div class="col-md-12 mb-1"><span class="font-weight-bold"><%=p.title%></span></div>
                    </div>

                    <div class="row">
                        <div class="col-md-12">
                        <img src="<%=p.image%>" class="img-fluid float-right mb-2 shadow ml-2" style="max-width: 64px;">
                            <%-p.description%>
                            <table class="table mt-2">
                                <thead class="bg-light text-black">
                                    <tr>
                                        <th>Ref</th>
                                        <th>Salary</th>
                                        <th>Closes</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td><%=p.reference%></td>
                                        <td><%=p.salary%></td>
                                        <td><%=p.closingdate%></td>
                                    </tr>
                                </tbody>
                            </table>

                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12">
                            <div class="mt-2">
                                <a href="#" target="_blank" class="btn btn-outline-secondary btn-sm float-right">More<i class="fa fa-chevron-circle-right ml-1"></i></a>
                            </div>
                        </div>
                    </div>

                    <hr />

                <% }); %>

            </div>
        </div>

    </div>

    <%- include('../partials/footer') %>

</body>
</html>
