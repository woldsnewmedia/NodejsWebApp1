<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>

    <header>
        <%- include('../partials/header') %>
    </header>

    <div class="main-outer-div">

        <div class="container">

            <h1>API QS Key</h1>

            <p>This is just to test using Node to get some test data from an API using a Key in the QueryString.</p>

            <p>API URL: <span class="font-weight-bold"><%=api_url%></span></p>

            <div class="card shadow">
                <div class="card-header">
                    API Jobs
                </div>
                <div class="card-body">
                    <% api_data.forEach((p, index) => { %>

                    <div class="row">
                        <div class="col-md-12 mb-1">
                            <h5 class="font-weight-bold"><%=p.title%></h5>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12">

                            <div class="float-right">
                                <img src="<%=p.image%>" class="img-fluid xxxfloat-right mb-2 shadow ml-2" style="max-width: 120px;">
                                <table class="table-apiqs m-2 w-100 p-2">
                                    <tr>
                                        <td>Ref</td>
                                        <td class="apiqs-p text-info"><%=p.reference%></td>
                                    </tr>
                                    <tr>
                                        <td>Salary</td>
                                        <td class="apiqs-p text-success"><%=p.salary%></td>
                                    </tr>
                                    <tr>
                                        <td>Closes</td>
                                        <td class="apiqs-p text-warning"><%=moment(p.closingdate).format("DD-MM-YYYY")%></td>
                                    </tr>
                                </table>
                            </div>

                            <%-p.description%>

                            <div class="clearfix">
                                <div class="row">
                                    <div class="col-md-12">
                                        <div class="mt-2">
                                            <a href="#" target="_blank" class="btn btn-outline-secondary btn-sm float-right">More<i class="fa fa-chevron-circle-right ml-1"></i></a>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>

                    <hr />

                    <% }); %>

                </div>
            </div>

        </div>

    </div>

    <%- include('../partials/footer') %>

</body>
</html>
